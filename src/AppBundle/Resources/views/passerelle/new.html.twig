{% extends 'base.html.twig' %}

{% block body %}
<div class="right_col" role="main">
    <h1>Nouvelle Passerelle</h1>


    <ul>
        <li>
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Cree" class="btn btn-success" />
            {{ form_end(form) }}
        </li>
        <li>
            <a href="{{ path('passerelle_index') }}" class="btn btn-primary">Retour a la liste</a>
        </li>
    </ul>
</div>
{% endblock %}
